# Marcus Baw's Homebrew tap

Formulas are auto-generated by [cargo-dist](https://opensource.axo.dev/cargo-dist/) on each release of the source projects.

## Usage

```bash
brew tap pacharanero/tap
brew install <formula>
```

## Formulas here

- [`dsc`](https://github.com/pacharanero/dsc) — Discourse CLI (installed binary is `dsc`; crate name is `dsc-rs`).
